@model int
@{
    ViewBag.Title = "Operator";
}

<h2>Operator</h2>

@(Html.Kendo().TabStrip().Name("Actions")
    .Items(
    items =>
    {
        items.Add().Text("Approval Requests").LoadContentFrom("ApprovalRequests", "Credit").Selected(true);
        items.Add().Text("Clients confirmation").LoadContentFrom("UnconfirmedClients", "Operator");
        items.Add().Text("Clients").LoadContentFrom("ApprovedClients", "Clients");
    }
      ))